import { createAction } from 'typesafe-actions';

export const addGroup = createAction('NAVIGATION_ADD_GROUP');
export const addTopic = createAction('NAVIGATION_ADD_TOPIC');
export const home = createAction('NAVIGATION_HOME');
export const signup = createAction('NAVIGATION_SIGNUP');
export const topics = createAction('NAVIGATION_TOPICS');
export const waiting = createAction('NAVIGATION_WAITING');
